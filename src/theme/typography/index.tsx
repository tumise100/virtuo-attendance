import { View, Text } from "react-native";
import React from "react";

export const HeadingsSemibold48 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-5xl leading-[50px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const HeadingsSemibold40 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[40px] leading-[46px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};
export const HeadingsSemibold36 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[36px] leading-[42px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const HeadingsSemibold30 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[30px] leading-[40px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const HeadingsSemibold24 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[24px] leading-[34px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const SubheadingSemibold20 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-semibold text-[20px] leading-[26px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const SubheadingSemibold18 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-semibold text-[18px] leading-[24px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const TextMedium16 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-normal text-[16px] leading-[24px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const TextMedium14 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[14px] leading-[22px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const TextBold14 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-bold text-[14px] leading-[22px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const TextQuote18 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-normal italic text-[18px] leading-[22px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const InputLabelMedium12 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[12px] leading-[22px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const InputPlaceholderMedium14 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[14px] leading-[22px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const InputAssistive = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[12px] leading-[16px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const ButtonCTAMedium16 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[16px] leading-[21px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const ButtonCTAlinksMedium18 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[18px] leading-[24px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const ButtonCTAsmallMedium14 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[14px] leading-[19px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const CaptionMedium12 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[12px] leading-[16px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const OverlineMedium10 = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[10px] leading-[14px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const ParagraphMediumMedium = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-medium text-[16px] leading-[24px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};

export const ParagraphSmallRegular = ({
  text,
  customClassName,
}: {
  text: string;
  customClassName?: string;
}) => {
  return (
    <Text
      className={`font-normal text-[14px] leading-[20px] text-textColor flex-shrink ${customClassName}`}
    >
      {text}
    </Text>
  );
};
